<?php
	# 4 scalar data types
	$integer = 1;
	$float = 1.1;
	$boolean = true;
	$string = "Hello World!";

	var_dump($integer);
	var_dump($float);
	var_dump($boolean);
	var_dump($string);

	# 4 compound data types
	$array = [1, 1.1, true, "Hello World!"];
	# object - Later
	# callable - Later
	# iterable - Later

	# 2 special data types
	$null = null;
	# resource - Later
